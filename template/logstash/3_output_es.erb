output {

    elasticsearch {
        codec => "json"
        host => "<%= ENV['OPENSHIFT_LOGSTASH_ES_HOST'] %>"
        idle_flush_time => 1
        protocol => "http"
        workers => 1
      }
}
